// 4.18.  แบบฝึกหัด
// 5.	จงเขียนฟังก์ชัน multiplyNumeric(obj, times) โดยถ้า properties นั้น
// มี value เป็น number ให้คุณ value นั้นด้วย times ถ้าข้อมูลเเป็นอย่างอื่นไม่ต้องทำอะไร

// before the call
let menuBefore = {
    width: 200,
    height: 300,
    title: "My menu"
};

// after the call
menuAfter = {
    width: 400,
    height: 600,
    title: "My menu"
}

let multiplyNumeric();
