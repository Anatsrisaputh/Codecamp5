// เขียนฟังก์ชัน sumInput() ที่
// ใช้ propmt รับ value มาเก็บใน array
// หยุดถามเมื่อเจอค่าที่ไม่ใช่ ตัวเลข
// คำนวณผลรวมของตัวเลขทั้งหมดใน Array

let insert = []
function sumInput() {
    for (let insert = 0; ;) {
        let insert = Number(prompt ("Please insert number"));
    if (insert !== Number) 
    break;
 }
   let summary = summary + insert;
    alert(summary);
}

alert(sumInput());


